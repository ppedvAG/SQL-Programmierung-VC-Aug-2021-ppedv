select 
	SP as SPA,
	'TXT', ZAHL
	F()
	Mathe
	(SELECT)--nur ein Wert darf rauskommen
FROM
	TAB t 
			inner|right|left JOIN Tab t2 ON t1.id = t2.id
			inner|right|left JOIN Tab t3
WHERE
		SP ><= != between in  Wert | f(Wert)
		AND OR
GROUP BY SP, SP  
	HAVING AGG(SP) > < 
ORDER BY SP1, SP2
	






--FROM-->JOINS (der Reihe nach)--> WHERE--> GROUP BY--> HAVING--> SELECT
--> ORDER BY --> TOP DISTINCT